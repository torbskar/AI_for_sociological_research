# Log: 2026-04-27
## Previous log: 2026-04-26.md

## .claudeignore accuracy fix (v6_draft_socarxiv.md)

### Decisions
Sentence in the "Empirical data and analysis" section corrected. Original text described `.claudeignore` as "enforcing" the immutability norm, which overstates what the tool does: `.claudeignore` excludes paths from the AI's working context but does not block the AI from executing file operations on those paths. Actual prevention of data reaching the API requires access-control configuration (e.g. `settings.json` requiring explicit approval for sensitive directories) and/or a `PreToolUse` hook.

Fix kept minimal: the sentence was rewritten to accurately describe `.claudeignore` as context-exclusion, with a brief clause noting that API-level protection requires additional configuration and a forward reference to the existing `PreToolUse` hook paragraph below. No new security detail was added to the section — the existing hook paragraph already covers the fuller picture.

The Appendix H folder tree annotation (line 782: "paths excluded from AI context") was confirmed accurate and left unchanged.

## PDF render (v6_draft_socarxiv.pdf)

### Decisions
v6_draft_socarxiv.pdf re-rendered via pandoc/xelatex using the same options as 2026-04-26.

Two issues diagnosed during render:
- MiKTeX "major issue: no administrator has checked for updates" — confirmed non-fatal; xelatex compiles successfully despite the warning. Permanent fix requires opening MiKTeX Console as administrator and running an update check once.
- Permission denied on the output PDF — caused by the existing v6_draft_socarxiv.pdf being open in a PDF viewer (file lock). Resolved by closing the viewer before re-running.

PDF confirmed written (190 KB) at `draft/v6_draft_socarxiv.pdf`.
